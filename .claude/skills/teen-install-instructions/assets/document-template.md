# <Guide Title>

## Purpose

<1-3 short paragraphs, written directly to the student. What can they do once this is done
that they couldn't before? Keep it concrete — "you'll be able to make an LED blink from your
own Python code" beats "sets up a development environment".>

## What gets installed

| Component | What it does                 | Why you need it           | Instructions source | Official docs    |
|-----------|------------------------------|---------------------------|---------------------|------------------|
| <name>    | <plain-language description> | <why this guide needs it> | [<label>](<url>)    | [<label>](<url>) |

## Target environment

- **Machine:** <e.g. Ubuntu 24.04 server / Windows 11 laptop / CircuitPython board model>
- **Minimum hardware:** <RAM, disk, ports, etc.>
- **Accounts / access needed:** <sudo? admin? an account on some service? none?>
- **Network:** <normal internet access assumed / specific ports or hosts required>
- **Before you start, you should already have:** <anything this guide assumes>

## Installation

### On <target 1> — <what we're doing here and why>

<2-3 sentences of plain-language context.>

#### <Major block name>

```bash
# <comment explaining what this line does and why, in plain language>
<command>

# <next comment>
<command>
```

**Test it**

```bash
# confirms the install worked
<tool> --version

# wherever possible, also run it against real/sample data, not just a version check
<command using sample data>
```

**Clean up**

```bash
# remove anything created just for the test above
<command>
```

<!-- Repeat "#### <Major block name>" blocks as needed within this target section. -->

### On <target 2> — <what's changing and why we're switching gears>

<same structure as above, repeated per target/tool as needed.>

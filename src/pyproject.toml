[project]
name = "src"
version = "0.1.0"
description = "wire frame program for class 4"
readme = "README.md"
requires-python = ">=3.14.0"
dependencies = ["matplotlib>=3.11.2", "numpy>=2.5.3", "pyserial>=3.5"]
